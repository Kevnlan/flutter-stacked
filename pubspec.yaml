name: flutter_application_stacked
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  get_it: ^7.6.4
  injectable: ^2.3.2
  http: ^1.1.0
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0
  sizer: ^2.0.15
  graphql_flutter: ^5.2.0-beta.6
  socket_io_client: ^2.0.3+1
  web_socket_channel: ^2.4.0

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

  assets:
    - assets/.env


